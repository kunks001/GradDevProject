<% n = 0 %>
<%= f.fields_for :questions do |question_f| %>
  
  <fieldset>

    <%= question_f.label :query, "Question #{n}" %>

    <%= question_f.text_field :query %>

    <%= render 'answer_fields', f: question_f %>
    <% n += 1 %>
  </fieldset>

<% end %>