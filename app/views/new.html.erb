<% if @quiz.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@quiz.errors.count, "error") %> prohibited this post from being saved:</h2>
 
    <ul>
    <% @quiz.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for @quiz do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title, placeholder: 'Your title here' %>
  <%= f.label :number_of_quesitons %>
  <%= f.text_field :number_of_quesitons, placeholder: 'number of quesitons you want to set up in this quiz' %>

  <%= render 'question_fields', f: f %>

  <%= f.submit %>

<% end %>